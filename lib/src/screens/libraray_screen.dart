import 'package:flutter/material.dart';

class LibrarayScreen extends StatelessWidget {
  const LibrarayScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      color: Colors.yellow, // Full-screen yellow box
      width: double.infinity,
      height: double.infinity,
    );
  }
}
